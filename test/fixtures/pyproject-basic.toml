[project]
name = "test-python-project"
description = "A test Python project for metadata sync"
keywords = ["python", "testing", "sync"]

[project.urls]
homepage = "https://python-example.com"
repository = "https://github.com/test/python-repo"

[tool.poetry]
name = "poetry-project"
version = "0.1.0"
description = "Poetry project description"
homepage = "https://poetry-example.com"
repository = "https://github.com/test/poetry-repo"
keywords = ["poetry", "metadata"]
